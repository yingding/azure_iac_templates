# remove the terraform state file to be synced to the git repo  
*.tfstate
# remove the .auto.tfvars to not sync the provider secrets to the git repo
.auto.tfvars
.terraform/
.terraform.lock.hcl
# terraform.tfstate.lock.info generated by terraform plan
*.tfstate.lock.info
# no state file should be present in the git repo
terraform.tfstate

# use a template
terraform.tfvars
**/*.tfstate.backup

# Not syncing 
nonprod_principle_contributor.json
provider.tf
**/*.env
**/envs/*.env 
**/envs/.env_template.env
!**/envs/.env_template.env 


